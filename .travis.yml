os: linux
dist: focal
language: java
jdk:
  - openjdk8
jobs:
  include: 
  - name: “test” # existing functionality
    script: # existing functionality
      - java -version
  - name: “sbom”
    script: # existing functionality
      - java -version
      - cd /java/src/main/java/hello/HelloWorld.java /test
    addons:
      sbom:
        on:
          branch: 'main'
          pr: false
        # condition: false
        run_phase: "before_script"
        output_format: "cyclonedx-json"
        output_dir: "/test"
        input_dir: "/java"
